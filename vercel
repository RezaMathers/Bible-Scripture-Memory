# Initialize git in your project folder
git init
git add .
git commit -m "Initial commit"

# Create a new repository on GitHub through the website
# Then connect your local repo (replace YOUR-USERNAME and REPO-NAME)
git remote add origin https://github.com/RezaMathers/Bible-Scripture-Memory.git
git branch -M main
git push -u origin main
